check asterisk with pidfile /var/run/asterisk/asterisk.pid
        if mem > 900.0 MB for 5 cycles then restart
        if cpu > 95% for 5 cycles then restart
        start program = "systemctl start asterisk.service"
        stop  program = "systemctl stop asterisk.service"

